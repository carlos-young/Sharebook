<h1>Sessions#index</h1>
<p>Here should be the sign in page</p>

<%= form_for @sessions do |session| %>
<p>
<%= session.label :email%><br />
<%= session.text_field :email%><br />
<%= session.label :password%><br />
<%= session.text_field :password%>
</p>
<% end%>
